const getBurgerMenu = state => state.toggleComponents.burgerMenu;

const getModalWindow = state => state.toggleComponents.modalWindow;

const getSpinner = state => state.toggleComponents.spinner;

export default {
  getBurgerMenu,
  getModalWindow,
  getSpinner,
};